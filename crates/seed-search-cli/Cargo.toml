[package]
name = "seed-search-cli"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
gogma-rng-core = { path = "../rng-core" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
